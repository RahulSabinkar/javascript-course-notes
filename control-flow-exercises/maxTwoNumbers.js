// Takes two numbers and returns the maximum of the two
const num1 = 10;
const num2 = 15;

function maxOfTwoNumbers(num1, num2) {
  return num1 > num2 ? num1 : num2;
}

console.log(maxOfTwoNumbers(num1, num2));
